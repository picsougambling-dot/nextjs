import type { Metadata } from "next";
import { siteConfig } from "@/config/site";
export const metadata: Metadata = {
  title: "Avis SlotRush Casino 2025 Retraits Express | GigaBonus",
  description: "Avis détaillé SlotRush Casino 2025. Retraits express, slots premium et bonus turbo.",
  alternates: { canonical: `${siteConfig.url}/blog/avis-slotrush-2025` },
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }