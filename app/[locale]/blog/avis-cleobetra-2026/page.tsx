import Page from '../avis-cleobetra-2025/page';

export default function AvisCleobetra2026Page() {
  const locale = useLocale();
  const tBlog = useTranslations('BlogPages.avis-cleobetra-2026');
  const tCommon = useTranslations('Common');
  return <Page />;
}
