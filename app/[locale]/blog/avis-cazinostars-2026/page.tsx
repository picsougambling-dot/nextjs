import Page from '../avis-cazinostars-2025/page';

export default function AvisCazinostars2026Page() {
  const locale = useLocale();
  const tBlog = useTranslations('BlogPages.avis-cazinostars-2026');
  const tCommon = useTranslations('Common');
  return <Page />;
}
