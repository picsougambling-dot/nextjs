import Page from '../avis-roibets-2025/page';

export default function AvisRoibets2026Page() {
  const locale = useLocale();
  const tBlog = useTranslations('BlogPages.avis-roibets-2026');
  const tCommon = useTranslations('Common');
  return <Page />;
}
